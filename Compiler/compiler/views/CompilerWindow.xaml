<Window x:Class="Compiler.CompilerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Compiler"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:Compiler.compiler.viewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:CompilerViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="NotMaterialDesignStyle" TargetType="RichTextBox"></Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="6*" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>
        <!--Topbar menu -->
        <Menu>
            <MenuItem Header="{StaticResource FileHeader}">
                <MenuItem Header="{StaticResource CreateHeader}" />
                <MenuItem Header="{StaticResource OpenHeader}" />
                <MenuItem Header="{StaticResource SaveHeader}" />
                <MenuItem Header="{StaticResource SaveAsHeader}" />
                <MenuItem Header="{StaticResource ExitHeader}" />
            </MenuItem>

            <MenuItem Header="{StaticResource EditHeader}">
                <MenuItem Header="{StaticResource CancelHeader}" />
                <MenuItem Header="{StaticResource RepeatHeader}" />
                <MenuItem Header="{StaticResource CutHeader}" />
                <MenuItem Header="{StaticResource CopyHeader}" />
                <MenuItem Header="{StaticResource PutHeader}" />
                <MenuItem Header="{StaticResource DeleteHeader}" />
                <MenuItem Header="{StaticResource SelectAllHeader}" />
            </MenuItem>

            <MenuItem Header="{StaticResource TextHeader}">
                <MenuItem Header="{StaticResource TaskHeader}" />
                <MenuItem Header="{StaticResource GrammarHeader}" />
                <MenuItem Header="{StaticResource GrammarClassificationHeader}" />
                <MenuItem Header="{StaticResource AnalyzationMethodHeader}" />
                <MenuItem Header="{StaticResource DiagnosisHeader}" />
                <MenuItem Header="{StaticResource ExampleHeader}" />
                <MenuItem Header="{StaticResource BooksHeader}" />
                <MenuItem Header="{StaticResource SourceCodeHeader}" />
            </MenuItem>

            <MenuItem Header="{StaticResource StartHeader}" />

            <MenuItem Header="{StaticResource ReferenceHeader}">
                <MenuItem Header="{StaticResource CallReferenceHeader}" />
                <MenuItem Header="{StaticResource ProgrammDescriptionHeader}" />
            </MenuItem>
        </Menu>

        <!--Button menu -->
        <ToolBarTray Grid.Row="1">
            <ToolBar
                ClipToBounds="False"
                Style="{StaticResource MaterialDesignToolBar}">

                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=FileDocumentAdd}"
                    ToolTip="{StaticResource CreateHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=FileSearch}"
                    ToolTip="{StaticResource OpenHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=ContentSave}"
                    ToolTip="{StaticResource SaveHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=ArrowLeft}"
                    ToolTip="{StaticResource CancelHeader}" />

                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=Repeat}"
                    ToolTip="{StaticResource RepeatHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=ContentCopy}"
                    ToolTip="{StaticResource CopyHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=ContentCut}"
                    ToolTip="{StaticResource CutHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=Import}"
                    ToolTip="{StaticResource PutHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=Play}"
                    ToolTip="{StaticResource StartHeader}" />
                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=HelpCircle}"
                    ToolTip="{StaticResource CallReferenceHeader}" />

                <Button
                    Command="{Binding}"
                    Content="{materialDesign:PackIcon Kind=InformationVariantCircle}"
                    ToolTip="{StaticResource ProgrammDescriptionHeader}" />

            </ToolBar>
        </ToolBarTray>

        <!--Text editor -->
        <RichTextBox Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                     Style="{StaticResource NotMaterialDesignStyle}" />

        <DataGrid
            Grid.Row="3"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserReorderColumns="False"
            CanUserResizeColumns="False"
            CanUserResizeRows="False"
            CanUserSortColumns="False"
            HeadersVisibility="All"
            ItemsSource="{Binding CompilationErrors}">
            <DataGrid.Columns>
                <materialDesign:DataGridTextColumn
                    Binding="{Binding Index}"
                    Header="{StaticResource IndexHeader}">
                </materialDesign:DataGridTextColumn>
                
                <materialDesign:DataGridTextColumn
                    Binding="{Binding FilePath}"
                    Header="{StaticResource FilePathHeader}"
                    Width="5*" />

                <materialDesign:DataGridTextColumn
                    Binding="{Binding Line}"
                    Header="{StaticResource LineHeader}">
                </materialDesign:DataGridTextColumn>

                <materialDesign:DataGridTextColumn
                    Binding="{Binding Column}"
                    Header="{StaticResource ColumnHeader}">
                </materialDesign:DataGridTextColumn>

                <materialDesign:DataGridTextColumn
                    Binding="{Binding Message}"
                    Header="{StaticResource MessageHeader}"
                    Width="4*">
                </materialDesign:DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>